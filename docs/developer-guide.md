# Developer Guide

## Adding Rules

1. Extend the rule engine in src/rule-engine.ts.
2. Add a test case in tests/.
3. Run npm test to verify behavior.

## Extending the Review Report

Add new fields to src/types.ts and populate them in src/index.ts.
